services:

  # ── PostgreSQL ───────────────────────────────────────────────────────────────
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: grader
      POSTGRES_PASSWORD: grader
      POSTGRES_DB: grader
    volumes:
      - grader-db-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U grader -d grader"]
      interval: 5s
      timeout: 5s
      retries: 10

  # ── Piston code execution engine ─────────────────────────────────────────────
  piston:
    image: ghcr.io/engineer-man/piston
    restart: unless-stopped
    privileged: true
    volumes:
      - piston-data:/piston
    healthcheck:
      test: ["CMD-SHELL", "bash -c '</dev/tcp/localhost/2000'"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 15s

  # ── Install language runtimes into Piston (runs once, exits) ─────────────────
  # Installs Python and C. The C *install* package is `gcc` (the execute runtime
  # is exposed as `c`) — verified vs the engine in #61. Re-runs safely on each
  # `up`: an already-installed package still resolves the fetch, so we exit 0
  # unless a request truly fails to reach Piston. Keep versions in sync with
  # src/lib/languages.ts (guarded by languages.docker.test.ts).
  piston-init:
    image: node:20-alpine
    depends_on:
      piston:
        condition: service_healthy
    command: >-
      node -e "const pkgs=[{language:'python',version:'3.10.0'},{language:'gcc',version:'10.2.0'}];(async()=>{for(const p of pkgs){const r=await fetch('http://piston:2000/api/v2/packages',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(p)});console.log(p.language,p.version,r.status,await r.text())}})().catch(e=>{console.error(e);process.exit(1)})"

  # ── Schema setup + seed (runs once, exits) ──────────────────────────────────
  migrate:
    build:
      context: .
      target: migrator
    command: npx tsx scripts/setup-db.ts
    depends_on:
      db:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://grader:grader@db:5432/grader
      SEED_ADMIN_EMAIL:    ${SEED_ADMIN_EMAIL:-admin@kmitl.ac.th}
      SEED_ADMIN_PASSWORD: ${SEED_ADMIN_PASSWORD:-Password123!}
      SEED_ADMIN_NAME:     ${SEED_ADMIN_NAME:-System Admin}

  # ── Next.js app ──────────────────────────────────────────────────────────────
  app:
    build:
      context: .
      target: runner
    restart: unless-stopped
    ports:
      - "${APP_PORT:-3000}:3000"
    depends_on:
      db:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
      piston-init:
        condition: service_completed_successfully
    environment:
      DATABASE_URL:         postgresql://grader:grader@db:5432/grader
      SESSION_SECRET:       ${SESSION_SECRET:?SESSION_SECRET is required — set it in .env}
      NEXTAUTH_URL:         ${NEXTAUTH_URL:-http://localhost:3000}
      GOOGLE_CLIENT_ID:     ${GOOGLE_CLIENT_ID:-}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET:-}
      PISTON_URL:           http://piston:2000/api/v2
      ANTHROPIC_API_KEY:    ${ANTHROPIC_API_KEY:-}

volumes:
  grader-db-data:
  piston-data:
